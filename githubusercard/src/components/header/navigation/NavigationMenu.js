import React from 'react';

const NavigationMenu = props => {
    return (
        <>
            <nav>
                <a href="#">Home</a>
            </nav>
        </>
    );
};

export default NavigationMenu;